- Converting nextjs-webapp to react native app

```
provide me a proper react-native community cli clone of this nextjs application,
utilize the APIs integrations, analyze all of the enpoints and functionality from the nextjs application, and do integrate them in our react native community cli application.
utilize the proper alternate working libraries of react native applications for icons, file picking, calender, etc.
do check the libraries and all of the features or accessibilities included in the existing nextjs app, and from that included everything properly which should be react native compatible and working)
use the proper colorscheme, typography, styling and layout like we have in our nextjs app but should utilize the proper react-native standards and not going to conflict or provide some errors regarding not following any standard way to use the library components.
use the best modern layout for the app, should not be boring styled app, and things shouldn't break to open or ux of the app should not be boring (improvise this!)
properly use the assets like we've used in the nextjs-webapp for splashscreen, logos and all
use the most stable and functional things of react native community cli associated libraries to make sure everything works with 99% of assurity.
and the APIs and all of the functionality of the app, I need this to be properly integrated and working.
```
